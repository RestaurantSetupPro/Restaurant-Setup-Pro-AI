# Module 05.1 README

运行 `npm install`、`npm start`，登录 Admin/Owner/Designer，进入 Products → Product Detail → AI Content Factory。

测试使用 `npm test`。验收重点：源图片、三种模式、草稿编辑、Approve/Reject、Apply、图片任务、Sales 只读和 Debug Center。

Migration：`database/migrations/003_ai_product_content_factory.sql`。详细规范：`docs/AI_PRODUCT_CONTENT_FACTORY.md`。

本版本不连接真实 AI 或生成真实图片。生成资料必须人工审核。
