# Module 05.2 README

Run `npm install`, `npm start`, then open Product Detail → AI Content Factory. Mock is the safe default. Configure optional OpenAI values from `.env.example` without committing secrets.

Run `npm test`. Verify provider status, task execution, preview, retry, review and Apply. Generated local files are ignored by Git and must use durable object storage in production.
